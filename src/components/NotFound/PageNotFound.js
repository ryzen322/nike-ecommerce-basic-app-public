import classes from "./PageNotFound.module.scss";

const PageNotFound = () => {
  return (
    <section className={classes["notFound-section"]}>
      <h1>Page Not Found!</h1>
    </section>
  );
};

export default PageNotFound;
